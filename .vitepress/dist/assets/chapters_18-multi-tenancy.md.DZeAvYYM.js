import{_ as o,o as t,c as r,ag as c}from"./chunks/framework.BZohXCq9.js";const l=JSON.parse('{"title":"第18章 多租户与安全隔离","description":"","frontmatter":{},"headers":[],"relativePath":"chapters/18-multi-tenancy.md","filePath":"chapters/18-multi-tenancy.md"}'),n={name:"chapters/18-multi-tenancy.md"};function s(a,e,d,i,p,u){return t(),r("div",null,[...e[0]||(e[0]=[c('<h1 id="第18章-多租户与安全隔离" tabindex="-1">第18章 多租户与安全隔离 <a class="header-anchor" href="#第18章-多租户与安全隔离" aria-label="Permalink to &quot;第18章 多租户与安全隔离&quot;">​</a></h1><blockquote><p>&quot;Security is not a product, but a process.&quot; —— Bruce Schneier</p></blockquote><p>沙箱的本质是一个安全边界。当多个租户共享同一套 OpenSandbox 基础设施时，每个租户的代码都可能包含恶意或有缺陷的逻辑。OpenSandbox 的安全设计遵循&quot;纵深防御&quot;原则——从容器运行时到 Linux 内核安全、从网络隔离到 API 认证、从输入验证到路径遍历防护，构建了多层交叉的安全屏障。</p><h2 id="_18-1-三种安全容器运行时" tabindex="-1">18.1 三种安全容器运行时 <a class="header-anchor" href="#_18-1-三种安全容器运行时" aria-label="Permalink to &quot;18.1 三种安全容器运行时&quot;">​</a></h2><p>OpenSandbox 支持三种安全容器运行时，定义在 <code>server/src/config.py</code> 的 <code>SecureRuntimeConfig</code> 类型中：</p><table tabindex="0"><thead><tr><th>运行时</th><th>隔离机制</th><th>Docker 支持</th><th>K8s 支持</th><th>适用场景</th></tr></thead><tbody><tr><td><strong>gVisor</strong></td><td>用户空间内核拦截系统调用</td><td>docker_runtime</td><td>k8s_runtime_class</td><td>高安全要求、低开销</td></tr><tr><td><strong>Kata Containers</strong></td><td>轻量级虚拟机</td><td>docker_runtime</td><td>k8s_runtime_class</td><td>VM 级隔离、兼容性好</td></tr><tr><td><strong>Firecracker</strong></td><td>microVM</td><td>不支持</td><td>k8s_runtime_class</td><td>极致安全、仅 K8s</td></tr></tbody></table><p><strong>为什么需要安全运行时？</strong> 标准 Linux 容器共享宿主机内核。如果容器内的代码利用内核漏洞实现逃逸，宿主机上所有容器都会受到威胁。gVisor 在用户空间实现了一个精简的内核接口，系统调用不直接到达宿主机内核；Kata 和 Firecracker 则通过虚拟化提供完全的内核隔离。</p><p>启动时，<code>validate_secure_runtime_on_startup()</code> 函数会验证配置的运行时是否在当前环境中可用。例如，如果配置了 gVisor 但 Docker 中未安装 runsc runtime，服务会在启动阶段就报错，而非等到创建沙箱时才失败。这种 fail-fast 策略避免了运行时的意外错误。</p><h2 id="_18-2-linux-安全加固" tabindex="-1">18.2 Linux 安全加固 <a class="header-anchor" href="#_18-2-linux-安全加固" aria-label="Permalink to &quot;18.2 Linux 安全加固&quot;">​</a></h2><p>即使不使用安全运行时，OpenSandbox 也通过多项 Linux 安全机制加固容器。这些配置在 <code>server/src/services/docker.py</code> 的 <code>_base_host_config_kwargs()</code> 方法和 <code>server/src/config.py</code> 的 <code>DockerConfig</code> 中定义：</p><p><strong>Capabilities 裁剪</strong>：Linux capabilities 将 root 权限拆分为细粒度的能力集。OpenSandbox 默认丢弃 9 个危险 capability，包括 <code>NET_RAW</code>（防止原始套接字嗅探）、<code>SYS_ADMIN</code>（防止挂载和 namespace 操作）等。只有 egress sidecar 容器被授予 <code>NET_ADMIN</code> 以管理网络策略，主容器从不获得此权限。</p><p><strong>no_new_privileges</strong>：<code>DockerConfig.no_new_privileges</code> 默认为 <code>True</code>。这个 Linux 内核标志确保进程及其子进程不能通过 execve 获取额外权限，即使执行了 setuid 二进制文件也无效。这堵住了容器内通过 SUID 提权的攻击路径。</p><p><strong>AppArmor 和 Seccomp profiles</strong>：可选配置。AppArmor 限制进程可以访问的文件路径和网络操作；Seccomp 限制进程可以调用的系统调用集合。两者结合可以将容器的攻击面收窄到最小必要范围。</p><p><strong>PID 限制</strong>：<code>pids_limit</code> 默认 512。这防止 fork 炸弹攻击——恶意代码无限 fork 进程以耗尽系统资源。512 个进程足以运行正常的开发和评估任务，但远不足以对宿主机构成威胁。</p><h2 id="_18-3-网络隔离与出站控制" tabindex="-1">18.3 网络隔离与出站控制 <a class="header-anchor" href="#_18-3-网络隔离与出站控制" aria-label="Permalink to &quot;18.3 网络隔离与出站控制&quot;">​</a></h2><p>网络隔离是多租户安全的关键防线。OpenSandbox 的网络策略实现了三个层次的控制，核心逻辑在 <code>components/egress/</code> 目录中：</p><p><strong>默认拒绝出站</strong>：沙箱容器默认不允许任何出站网络访问。这是最安全的默认策略——除非明确允许，否则容器无法访问外部网络。</p><p><strong>FQDN 粒度的访问控制</strong>：管理员可以配置允许访问的域名列表（而非 IP 地址）。这对 AI Agent 场景特别重要——Agent 可能需要访问特定 API（如 api.openai.com），但不应该能访问任意互联网地址。</p><p><strong>DNS 拦截防绕过</strong>：egress sidecar（<code>components/egress/nameserver.go</code>）在容器网络命名空间内运行 DNS 代理，拦截所有 DNS 查询。不在白名单中的域名查询直接返回 <code>NXDOMAIN</code>。这防止了通过硬编码 IP 地址绕过域名限制的攻击——在 <code>dns+nft</code> 模式下，<code>components/egress/nft.go</code> 使用 nftables 在内核层面只允许 DNS 解析出的 IP 通过，并按 TTL 自动过期。</p><p>这种 DNS 层 + 内核层的双重防线设计，是 OSEP-0001 (FQDN-based Egress Control) 提案的核心贡献。</p><h2 id="_18-4-三层-api-认证体系" tabindex="-1">18.4 三层 API 认证体系 <a class="header-anchor" href="#_18-4-三层-api-认证体系" aria-label="Permalink to &quot;18.4 三层 API 认证体系&quot;">​</a></h2><p>OpenSandbox 的三个组件（Lifecycle API、execd、egress）各有独立的认证机制，形成纵深的身份验证链：</p><p><strong>第一层：Lifecycle API Key</strong>。Header 名为 <code>OPEN-SANDBOX-API-KEY</code>，在 <code>server/src/middleware/auth.py</code> 的 <code>AuthMiddleware</code> 中实现。健康检查（<code>/health</code>）、API 文档（<code>/docs</code>、<code>/redoc</code>）和代理路径被豁免认证。代理路径通过严格的正则表达式 <code>^(/v1)?/sandboxes/[^/]+/proxy/\\d+(/|$)</code> 匹配，拒绝包含 <code>..</code> 的路径遍历尝试。</p><p><strong>第二层：execd Access Token</strong>。Header 名为 <code>X-EXECD-ACCESS-TOKEN</code>，令牌在沙箱创建时由 Lifecycle API 自动生成并注入到容器环境变量中。这确保只有通过正当途径创建的沙箱才能接受执行命令——即使攻击者发现了 execd 的端口，没有令牌也无法执行任何操作。</p><p><strong>第三层：Egress Auth Token</strong>。Header 名为 <code>OPENSANDBOX-EGRESS-AUTH</code>，用于保护 egress sidecar 的策略 API。关键实现细节是使用<strong>常量时间比较</strong>（constant-time comparison）验证令牌，防止时序攻击——攻击者无法通过测量响应时间来逐字符猜测令牌。</p><p><strong>为什么每层使用不同的 Header 名？</strong> 这是有意为之的安全设计。如果三层使用同一个认证机制，一旦某层的令牌泄露，攻击者就能访问所有层。独立的令牌确保了安全域的分离——即使 execd 令牌泄露，攻击者也无法修改 egress 策略。</p><h2 id="_18-5-输入验证与注入防护" tabindex="-1">18.5 输入验证与注入防护 <a class="header-anchor" href="#_18-5-输入验证与注入防护" aria-label="Permalink to &quot;18.5 输入验证与注入防护&quot;">​</a></h2><p><code>server/src/services/validators.py</code> 包含了全面的输入验证逻辑，覆盖了多个攻击向量：</p><p><strong>OSSFS 注入防护</strong>：OSSFS 挂载需要构建命令行参数，包括 bucket 名、endpoint、挂载选项等。如果这些参数未经验证就拼接到命令中，攻击者可以注入额外的命令。验证器对 bucket 名强制 DNS 命名规范，对 endpoint 验证 URL 格式，对挂载选项检查是否包含危险字符。<code>server/src/services/ossfs_mixin.py</code> 中的 OSSFSMixin 通过 14 个方法实现了从路径解析到命令构建的完整安全链。</p><p><strong>Host Path 白名单</strong>：Docker 后端的 bind mount 通过 <code>StorageConfig.allowed_host_paths</code> 配置允许挂载的宿主机路径前缀。验证器检查请求的路径是否以白名单中的某个前缀开头，拒绝非规范化路径（如包含 <code>//</code> 的路径）和路径遍历（<code>..</code>）。</p><p><strong>镜像认证</strong>：<code>ImageAuth</code> 类型支持私有镜像仓库的认证信息。验证器确保认证数据格式正确，防止格式错误的认证信息导致的安全问题。</p><p><strong>保留标签前缀</strong>：以 <code>opensandbox.io/</code> 为前缀的标签是系统保留标签。验证器拒绝用户设置这些标签，防止通过伪造系统标签来干扰内部逻辑。</p><h2 id="_18-6-多租户隔离架构" tabindex="-1">18.6 多租户隔离架构 <a class="header-anchor" href="#_18-6-多租户隔离架构" aria-label="Permalink to &quot;18.6 多租户隔离架构&quot;">​</a></h2><p>在 Kubernetes 后端，OpenSandbox 通过多重机制实现租户隔离：</p><p><strong>容器级隔离</strong>：每个沙箱运行在独立的 Pod 中。即使两个租户的沙箱调度到同一个节点，Linux namespace 和 cgroup 也确保它们互不可见。</p><p><strong>Namespace 隔离</strong>：通过 <code>KubernetesRuntimeConfig</code> 中的 namespace 配置，不同租户的沙箱可以运行在不同的 Kubernetes namespace 中。结合 NetworkPolicy，namespace 之间的网络流量被完全隔离。</p><p><strong>ServiceAccount 配置</strong>：<code>KubernetesRuntimeConfig.service_account</code> 允许为沙箱 Pod 指定最小权限的 ServiceAccount。默认的 ServiceAccount 不应有任何 RBAC 权限，确保沙箱内的代码无法通过 Kubernetes API 访问集群资源。</p><p><strong>资源配额</strong>：通过 Kubernetes 的 ResourceQuota 机制（在 namespace 层面配置），限制每个租户可以使用的总 CPU、内存和 Pod 数量，防止单个租户耗尽集群资源。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>OpenSandbox 的安全设计体现了&quot;纵深防御&quot;的核心理念。三种安全容器运行时提供了从 syscall 拦截到 VM 隔离的不同安全级别；Linux 安全机制（capabilities 裁剪、no_new_privileges、AppArmor、Seccomp、PID 限制）在内核层面收窄攻击面；网络隔离通过 DNS 拦截和 nftables 实现 FQDN 粒度的出站控制；三层独立的 API 认证阻止了跨安全域的攻击扩散；输入验证则在应用层堵住了注入和遍历攻击。理解这些安全层次之间的协作关系，对于在生产环境中安全运行多租户沙箱至关重要。</p>',40)])])}const h=o(n,[["render",s]]);export{l as __pageData,h as default};
