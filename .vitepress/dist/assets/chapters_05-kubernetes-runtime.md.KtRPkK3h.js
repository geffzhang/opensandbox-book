import{_ as o,o as a,c as s,ag as n}from"./chunks/framework.BZohXCq9.js";const k=JSON.parse('{"title":"第5章 Kubernetes 运行时后端","description":"","frontmatter":{},"headers":[],"relativePath":"chapters/05-kubernetes-runtime.md","filePath":"chapters/05-kubernetes-runtime.md"}'),t={name:"chapters/05-kubernetes-runtime.md"};function i(r,e,d,c,l,p){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="第5章-kubernetes-运行时后端" tabindex="-1">第5章 Kubernetes 运行时后端 <a class="header-anchor" href="#第5章-kubernetes-运行时后端" aria-label="Permalink to &quot;第5章 Kubernetes 运行时后端&quot;">​</a></h1><blockquote><p>&quot;Kubernetes doesn&#39;t just run your containers — it becomes the operating system of your distributed application.&quot; — Kelsey Hightower</p></blockquote><h2 id="从-docker-到-kubernetes-复杂度跃迁" tabindex="-1">从 Docker 到 Kubernetes：复杂度跃迁 <a class="header-anchor" href="#从-docker-到-kubernetes-复杂度跃迁" aria-label="Permalink to &quot;从 Docker 到 Kubernetes：复杂度跃迁&quot;">​</a></h2><p>如果说 Docker 后端是&quot;一个人的战斗&quot;（单文件 2132 行），那么 Kubernetes 后端就是&quot;一支军队的协作&quot; — 15 个文件，涵盖 API 客户端、Informer 缓存、速率限制、工作负载抽象、CRD 管理等多个子系统。</p><p>为什么复杂度会急剧增加？根本原因在于 Kubernetes 的分布式特性。Docker 后端直接与本地 Docker daemon 通信，状态完全由自身管理。而 Kubernetes 后端面对的是一个最终一致的分布式系统 — API Server 可能过载、Watch 连接可能断开、Pod 调度可能延迟、资源版本可能过期。每一个故障模式都需要专门的应对机制。</p><h2 id="核心类层次" tabindex="-1">核心类层次 <a class="header-anchor" href="#核心类层次" aria-label="Permalink to &quot;核心类层次&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>KubernetesSandboxService</span></span>
<span class="line"><span>  └── K8sClient (lazy init)</span></span>
<span class="line"><span>        ├── CoreV1Api / CustomObjectsApi / NodeV1Api</span></span>
<span class="line"><span>        ├── WorkloadInformer (缓存)</span></span>
<span class="line"><span>        └── TokenBucketRateLimiter (限流)</span></span>
<span class="line"><span>  └── WorkloadProvider (抽象)</span></span>
<span class="line"><span>        ├── BatchSandboxProvider</span></span>
<span class="line"><span>        └── AgentSandboxProvider</span></span>
<span class="line"><span>  └── SecureRuntimeResolver</span></span></code></pre></div><p><code>KubernetesSandboxService</code>（定义在 <code>server/src/services/k8s/kubernetes_service.py</code>）实现了 <code>SandboxService</code> 接口，但将具体的工作负载操作委托给 <code>WorkloadProvider</code>。<code>K8sClient</code>（<code>k8s/client.py</code>）封装了所有 Kubernetes API 调用，提供统一的速率限制和缓存策略。</p><h2 id="k8sclient-统一的-api-访问层" tabindex="-1">K8sClient：统一的 API 访问层 <a class="header-anchor" href="#k8sclient-统一的-api-访问层" aria-label="Permalink to &quot;K8sClient：统一的 API 访问层&quot;">​</a></h2><p><code>K8sClient</code> 的设计体现了几个重要的工程决策：</p><p><strong>懒加载 API 客户端</strong>。<code>CoreV1Api</code>、<code>CustomObjectsApi</code> 和 <code>NodeV1Api</code> 三个客户端在首次使用时才初始化（lazy init），而非构造时创建。这避免了在配置尚未就绪时触发连接错误，也减少了不使用某些 API 时的资源浪费。</p><p><strong>kubeconfig 与 ServiceAccount 双模式</strong>。客户端支持从 kubeconfig 文件加载配置（开发环境）或使用 Pod 内的 ServiceAccount token（生产环境），通过 <code>_load_config()</code> 自动检测。</p><p><strong>读写分离的速率限制</strong>。<code>K8sClient</code> 维护两个独立的 <code>TokenBucketRateLimiter</code> 实例（定义在 <code>k8s/rate_limiter.py</code>），分别控制读操作和写操作的 QPS。为什么要分离？因为在 Kubernetes 中，写操作（创建 Pod、更新 CRD）对 API Server 的压力远大于读操作。分离限流允许系统在写操作繁忙时仍然保持读操作的响应性。</p><p><strong>Informer 缓存优先</strong>。对于列表查询，<code>K8sClient</code> 首先尝试从 <code>WorkloadInformer</code> 的本地缓存中读取。只有当 Informer 尚未同步完成时，才回退到直接 API 调用。这个策略将列表查询的延迟从网络往返降低到内存访问。</p><h2 id="workloadinformer-python-版-list-watch" tabindex="-1">WorkloadInformer：Python 版 List-Watch <a class="header-anchor" href="#workloadinformer-python-版-list-watch" aria-label="Permalink to &quot;WorkloadInformer：Python 版 List-Watch&quot;">​</a></h2><p><code>k8s/informer.py</code> 中的 <code>WorkloadInformer</code> 是整个 Kubernetes 后端最精巧的组件。它用 Python 实现了 Kubernetes 原生的 List-Watch 模式 — 通常这种组件只在 Go 的 client-go 库中才能看到。</p><h3 id="缓存结构" tabindex="-1">缓存结构 <a class="header-anchor" href="#缓存结构" aria-label="Permalink to &quot;缓存结构&quot;">​</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_cache: Dict[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Dict[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Any]]  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 资源名 → 资源对象</span></span></code></pre></div><p>缓存使用 <code>RLock</code>（可重入锁）保护，允许同一线程嵌套获取锁，避免了递归调用时的死锁风险。</p><h3 id="三个核心循环" tabindex="-1">三个核心循环 <a class="header-anchor" href="#三个核心循环" aria-label="Permalink to &quot;三个核心循环&quot;">​</a></h3><p><strong><code>_full_resync()</code></strong> — 执行完整的 List 操作，用返回的资源列表替换整个缓存。这在启动时和 Watch 连接丢失时触发。</p><p><strong><code>_run_watch_loop()</code></strong> — 基于上次的 <code>resource_version</code> 建立 Watch 流，持续接收增量事件。这是稳态下的主要数据更新路径。</p><p><strong><code>_handle_event()</code></strong> — 处理三种事件类型：<code>ADDED</code> 和 <code>MODIFIED</code> 将对象 upsert 到缓存中，<code>DELETED</code> 从缓存中移除对象。</p><h3 id="资源版本的单调性检查" tabindex="-1">资源版本的单调性检查 <a class="header-anchor" href="#资源版本的单调性检查" aria-label="Permalink to &quot;资源版本的单调性检查&quot;">​</a></h3><p><code>_advance_resource_version()</code> 方法实现了一个巧妙的保护机制：它将 <code>resource_version</code> 当作不透明字符串处理（符合 Kubernetes API 约定），但尝试将其解析为整数进行比较。只有当新版本严格大于当前版本时才更新，防止时间倒流导致缓存状态回退。对于无法解析为整数的版本（理论上不应出现，但需要防御性编程），保守地跳过更新。</p><h3 id="指数退避与-410-gone-处理" tabindex="-1">指数退避与 410 Gone 处理 <a class="header-anchor" href="#指数退避与-410-gone-处理" aria-label="Permalink to &quot;指数退避与 410 Gone 处理&quot;">​</a></h3><p>当 Watch 连接出现错误时，Informer 使用 <strong>指数退避</strong> 重试：初始等待 1 秒，每次翻倍，上限 30 秒。成功后重置为 1 秒。</p><p>特别值得关注的是 <strong>410 Gone</strong> 错误的处理。这个错误表示 API Server 已经丢弃了请求的 <code>resource_version</code> 对应的事件历史（通常因为 etcd compaction）。Informer 的响应是触发一次完整的 <code>_full_resync()</code>，重建缓存并获取最新的 <code>resource_version</code>。这是 Kubernetes 控制器编程中的标准模式，OpenSandbox 在 Python 中忠实地复现了它。</p><h2 id="batchsandbox-crd" tabindex="-1">BatchSandbox CRD <a class="header-anchor" href="#batchsandbox-crd" aria-label="Permalink to &quot;BatchSandbox CRD&quot;">​</a></h2><p>Kubernetes 后端通过自定义资源定义（CRD）管理沙盒。<code>BatchSandbox</code> 是核心 CRD，其 spec 包含以下关键字段：</p><ul><li><strong>Replicas</strong> — 副本数，支持批量创建</li><li><strong>PoolRef</strong> — 关联的资源池引用（Pool 模式）</li><li><strong>Template</strong> — Pod 模板，包含容器定义、卷、资源限制</li><li><strong>ShardPatches</strong> — 分片补丁，支持按副本定制配置</li><li><strong>ExpireTime</strong> — 过期时间戳，由 Operator 监控并执行清理</li><li><strong>TaskTemplate</strong> — Pool 模式下的任务模板</li><li><strong>TaskResourcePolicyWhenCompleted</strong> — 任务完成后资源策略：<code>Retain</code>（保留）或 <code>Release</code>（释放）</li></ul><p>配套的 <code>Pool</code> CRD 定义了资源池的容量策略：<code>BufferMax/Min</code> 控制预热缓冲数量，<code>PoolMax/Min</code> 控制池的总体规模。Pool 模式通过预先创建容器显著降低了沙盒的冷启动延迟。</p><h2 id="batchsandboxprovider-模板与池双模式" tabindex="-1">BatchSandboxProvider：模板与池双模式 <a class="header-anchor" href="#batchsandboxprovider-模板与池双模式" aria-label="Permalink to &quot;BatchSandboxProvider：模板与池双模式&quot;">​</a></h2><p><code>k8s/batchsandbox_provider.py</code>（约 821 行）实现了 <code>WorkloadProvider</code> 接口，支持两种创建模式：</p><h3 id="模板模式-默认" tabindex="-1">模板模式（默认） <a class="header-anchor" href="#模板模式-默认" aria-label="Permalink to &quot;模板模式（默认）&quot;">​</a></h3><p>用户完全控制 Pod 定义 — 指定镜像、资源限制、环境变量、卷挂载和网络策略。Provider 在用户配置基础上注入 execd init container：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">initContainers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">execd-installer</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;execd-image&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    volumeMounts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">opensandbox-bin</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        mountPath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/opt/opensandbox/bin</span></span></code></pre></div><p>init container 将 execd 二进制和 <code>bootstrap.sh</code> 脚本复制到共享的 <code>emptyDir</code> 卷中。主容器的 entrypoint 被包装为 <code>[&quot;/opt/opensandbox/bin/bootstrap.sh&quot;] + original_entrypoint</code>，参数通过 <code>shlex.quote()</code> 转义防止 shell 注入。</p><h3 id="池模式" tabindex="-1">池模式 <a class="header-anchor" href="#池模式" aria-label="Permalink to &quot;池模式&quot;">​</a></h3><p>当 <code>extensions[&#39;poolRef&#39;]</code> 存在时激活。池模式引用预热的资源池，仅允许定制 entrypoint 和环境变量，<strong>不支持自定义卷</strong>。任务通过 <code>TaskSpec</code> 结构描述，bootstrap 脚本以后台模式运行：<code>&quot;/opt/opensandbox/bin/bootstrap.sh {entrypoint} &amp;&quot;</code>。</p><p>池模式的限制是有意为之的。预热池中的容器已经分配了资源和卷，运行时修改这些配置会打破池的同质性假设，导致调度和回收逻辑复杂化。</p><h2 id="沙盒创建等待与超时" tabindex="-1">沙盒创建等待与超时 <a class="header-anchor" href="#沙盒创建等待与超时" aria-label="Permalink to &quot;沙盒创建等待与超时&quot;">​</a></h2><p><code>KubernetesSandboxService</code> 中的 <code>_wait_for_sandbox_ready()</code> 实现了阻塞式轮询等待：</p><ul><li><strong>超时时间</strong>：通过 <code>app_config.kubernetes.sandbox_create_timeout_seconds</code> 配置，默认 60 秒</li><li><strong>轮询间隔</strong>：通过 <code>app_config.kubernetes.sandbox_create_poll_interval_seconds</code> 配置，默认 1.0 秒</li><li><strong>成功条件</strong>：工作负载状态为 <code>Running</code> 或 <code>Allocated</code>（已分配 IP）</li><li><strong>失败处理</strong>：超时后自动调用 <code>delete_workload()</code> 清理资源，返回 HTTP 504</li></ul><p>为什么选择轮询而非 Watch？因为创建等待是一个短期、低频的操作，为每次创建建立独立的 Watch 连接开销过大。而 Informer 的缓存更新有固有延迟，可能导致状态判断不够及时。直接轮询 API 在这个场景下是更简单可靠的选择。</p><h2 id="暂停-恢复-501-与未来计划" tabindex="-1">暂停/恢复：501 与未来计划 <a class="header-anchor" href="#暂停-恢复-501-与未来计划" aria-label="Permalink to &quot;暂停/恢复：501 与未来计划&quot;">​</a></h2><p>Kubernetes 后端的 <code>pause_sandbox()</code> 和 <code>resume_sandbox()</code> 均返回 <strong>HTTP 501 Not Implemented</strong>。这是一个诚实的技术决策 — Kubernetes 缺乏原生的 Pod 暂停/恢复语义。</p><p>OSEP-0008 提出了基于 <strong>rootfs 快照</strong> 的解决方案：将容器的文件系统状态快照保存，销毁 Pod 释放计算资源，恢复时从快照创建新 Pod。这种方式在 Kubernetes 的约束下实现了&quot;伪暂停&quot;，但涉及快照存储、状态一致性和恢复延迟等复杂问题，目前仍处于草案阶段。</p><h2 id="本章小结" tabindex="-1">本章小结 <a class="header-anchor" href="#本章小结" aria-label="Permalink to &quot;本章小结&quot;">​</a></h2><p>Kubernetes 后端通过 K8sClient 统一 API 访问、WorkloadInformer 实现 Python 版 List-Watch、BatchSandboxProvider 支持模板与池双模式，构建了一个完整的分布式沙盒管理系统。读写分离的速率限制、指数退避重试、410 Gone 自动重同步等机制体现了对 Kubernetes 分布式故障模式的深刻理解。暂停/恢复功能的 501 返回和 OSEP-0008 提案则展示了项目在技术诚实和渐进式演进之间的平衡。</p>`,50)])])}const u=o(t,[["render",i]]);export{k as __pageData,u as default};
